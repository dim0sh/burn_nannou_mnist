[package]
name = "burn_nannou_MNIST"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
burn = {version = "0.12.1", features = ["train", "wgpu", "dataset"]}
image = "0.25.0"
nannou = "0.19.0"
